#!/bin/sh
set -eu

SCRIPT_DIR=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
exec "${SCRIPT_DIR}/cerberus" eval "Application.ensure_all_started(:cerberus_elixir); IO.puts(\"No database migrations configured for cerberus_elixir\")"
