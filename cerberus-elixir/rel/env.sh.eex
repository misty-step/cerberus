#!/bin/sh

if [ -z "${CERBERUS_REPO_ROOT:-}" ]; then
  export CERBERUS_REPO_ROOT="${RELEASE_ROOT}/repo"
fi

if [ -z "${CERBERUS_DB_PATH:-}" ]; then
  export CERBERUS_DB_PATH="${RELEASE_ROOT}/data/cerberus.sqlite3"
fi

mkdir -p "$(dirname "${CERBERUS_DB_PATH}")"
