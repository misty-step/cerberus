# Required to run reviewers (OpenRouter)
OPENROUTER_API_KEY=

# Optional alias (action supports this env var)
CERBERUS_API_KEY=

# Optional overrides for local runs
OPENCODE_MODEL=
CERBERUS_FALLBACK_MODELS=
OPENCODE_MAX_STEPS=
REVIEW_TIMEOUT=

# Optional: auth for `gh` CLI in local scripts
GH_TOKEN=
